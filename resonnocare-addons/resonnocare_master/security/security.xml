<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Super Admin full access -->
    <record id="rule_crm_disposition_super_admin" model="ir.rule">
        <field name="name">CRM Disposition - Super Admin</field>
        <field name="model_id" ref="model_resonnocare_crm_disposition"/>
        <field name="domain_force">[(1,'=',1)]</field>
        <field name="groups" eval="[(4, ref('resonnocare_base.group_resonnocare_super_admin'))]"/>
    </record>

    <!-- Everyone else: read-only handled via access CSV -->
</odoo>
