<odoo>

    <record id="consent_tree" model="ir.ui.view">
        <field name="name">consent.tree</field>
        <field name="model">oeh.consent</field>
        <field name="arch" type="xml">
            <list decoration-success="state == 'signed'" decoration-warning="state == 'pending'"
                  decoration-muted="state in ('expired','cancel')">
                <field name="name"/>
                <field name="patient_id"/>
                <field name="consent_type"/>
                <field name="consent_date"/>
                <field name="signed"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <record id="consent_form" model="ir.ui.view">
        <field name="name">consent.form</field>
        <field name="model">oeh.consent</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_request" type="object" string="Request Signature"
                            class="btn-primary" invisible="state != 'draft'"/>
                    <button name="action_sign" type="object" string="Mark Signed"
                            class="btn-primary" invisible="state not in ('draft','pending')"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state in ('cancel',)"/>
                    <button name="action_reset" type="object" string="Reset"
                            invisible="state == 'draft'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,pending,signed"/>
                </header>
                <sheet>
                    <widget name="web_ribbon" title="Expired" bg_color="text-bg-danger"
                            invisible="not is_expired"/>
                    <group>
                        <group>
                            <field name="name" readonly="1"/>
                            <field name="patient_id"/>
                            <field name="consent_type"/>
                            <field name="procedure"/>
                            <field name="treatment_id"/>
                        </group>
                        <group>
                            <field name="consent_date"/>
                            <field name="valid_until"/>
                            <field name="guardian_name"/>
                            <field name="guardian_relation"/>
                            <field name="signed" readonly="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Consent Text">
                            <field name="consent_template" widget="html"/>
                        </page>
                        <page string="Signature">
                            <group>
                                <field name="signed_by"/>
                                <field name="signed_date"/>
                                <field name="signature" widget="image"/>
                            </group>
                        </page>
                        <page string="Notes">
                            <field name="notes"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="consent_kanban" model="ir.ui.view">
        <field name="name">consent.kanban</field>
        <field name="model">oeh.consent</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state">
                <field name="patient_id"/>
                <field name="consent_type"/>
                <field name="state"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click o_kanban_record">
                            <strong><field name="consent_type"/></strong>
                            <div><i class="fa fa-user"/> <field name="patient_id"/></div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="consent_action" model="ir.actions.act_window">
        <field name="name">Digital Consent</field>
        <field name="res_model">oeh.consent</field>
        <field name="view_mode">list,kanban,form</field>
    </record>

</odoo>
