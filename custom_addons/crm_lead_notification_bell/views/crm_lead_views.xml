<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="crm_lead_view_form_notification" model="ir.ui.view">
        <field name="name">crm.lead.view.form.notification</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_notifications" 
                        type="object" 
                        class="oe_stat_button" 
                        icon="fa-bell-o"
                        invisible="last_notification_count == 0">
                    <field name="last_notification_count" widget="statinfo" string="Notifications"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>